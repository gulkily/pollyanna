#!/usr/bin/perl -T

use strict;
use warnings;
use 5.010;

# get list of feeds to pull

# download file

# for each line, get the hash, see if it's in item table
# 	if not in item table, download it into txt directory and index it
#		sanity check: the hash is the same as reported by the feed
#			IndexFile() returns the resulting hash

1;
