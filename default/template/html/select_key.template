<!-- select_key.template -->
<!-- keychain.template -->
<fieldset>
    <label for=selectKey>
        Selected key:<br>
        <select name=selectKey id=selectKey onchange="selectLoadKey(this.value)">
            <option value=0>[No selection]</option>
        </select>
    </label>
</fieldset>
<!-- / select_key.template -->
